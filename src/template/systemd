[Unit]
Description={{{name}}}
After=network.target

[Service]
Type=simple
StandardOutput={{{stdOut}}}
StandardError={{{errOut}}}
ExecStart={{{deepstreamExec}}} {{{deepstreamArgs}}}
Restart=always
User={{{user}}}
Group={{{group}}}
Environment=

[Install]
WantedBy=multi-user.target
