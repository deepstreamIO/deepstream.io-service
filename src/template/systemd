[Unit]
Description={{{name}}}
After=network.target

[Service]
Type=simple
StandardOutput={{{stdOut}}}
StandardError={{{stdErr}}}
ExecStart={{{exec}}} {{{deepstreamArgs}}}
Restart=always
User={{{user}}}
Group={{{group}}}
Environment=

[Install]
WantedBy=multi-user.target
